[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-paths-question-suggestion-flow"
version = "0.1.0"
description = "Endpoints and structures to build question suggestion flow as a microservice"
dependencies = []