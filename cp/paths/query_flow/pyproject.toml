[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-cp-paths-query-flow"
version = "0.1.1"
description = "Paths for query flow microservice"
dependencies = [
    "pydantic>=2.11.5"
]
