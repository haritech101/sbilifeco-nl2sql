[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-paths-query-flow-answer-repo"
version = "0.2.0"
description = "REST API paths for query flow answer repository"
dependencies = []