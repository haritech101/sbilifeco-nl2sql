[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-cp-paths-llm"
version = "0.1.0"
description = "Paths for LLM microservice"
dependencies = []
