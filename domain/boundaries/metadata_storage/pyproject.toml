[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sbilifeco-boundaries-metadata-storage"
version = "0.1.0"
description = "Contract for metadata storage"
dependencies = [
    "sbilifeco-models-base>=0.1.0",
    "sbilifeco-models-db-metadata>=0.1.0"
]
