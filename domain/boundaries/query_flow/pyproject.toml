[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-boundary-query-flow"
version = "0.1.9"
description = "Contract for query flow"
dependencies = [
    "sbilifeco-models-base>=0.1.3"
]
